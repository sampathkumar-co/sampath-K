# ForgeShift public migration fixtures

This directory contains synthetic, public-safe Atlassian Connect descriptors used only to exercise official Connect-to-Forge migration tooling.

These fixtures contain no customer source code, credentials, secrets, production URLs, or private ForgeShift implementation details.

`connect-basic/atlassian-connect.json` mirrors the intentionally synthetic representative descriptor used by ForgeShift's migration certification flow.
